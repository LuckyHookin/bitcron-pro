   <div class="footer">
<div class="site_footer_wrap"><div class="site_footer">

      <div class="mysocials"><div class="my_socials">
		   <% ['telegram', 'twitter', 'weibo', 'github', 'envelope', 'rss'].forEach((item) => { %>
			   <% if (site.customConfig[item]) { %>
<a href="<%= site.customConfig[item] %>" title="<%= item %>"><i class="fa fa-<%= item %>"></i></a>
<% } %>
    <% }) %>
</div>
      </div>

      <div class="copyright" id="copyright">
	  <script>
		var date=new Date;
		var year=date.getFullYear(); 
		document.write("Copyright © "+year+" " + "<%= themeConfig.siteName %>. ");
		</script>
		Powered by Gridea.

		<br>
		本站总访问量为 <strong><span id="busuanzi_value_site_uv"></span></strong> 次
	  <%- themeConfig.footerInfo %>
      </div>
	  

    </div>

<script type="text/javascript" src="<%= themeConfig.domain %>/media/scripts/darkmode.js"></script>
<script>
var options = {
  bottom: '64px', // default: '32px'
  right: '32px', // default: '32px'
  left: 'unset', // default: 'unset'
  time: '0.5s', // default: '0.3s'
  mixColor: '#fff', // default: '#fff'
  backgroundColor: '#f7f7f7',  // default: '#fff'
  buttonColorDark: '#100f2c',  // default: '#100f2c'
  buttonColorLight: '#fff', // default: '#fff'
  saveInCookies: false, // default: true,
  label: '🌓', // default: ''
  autoMatchOsTheme: false // default: true
}

const darkmode = new Darkmode(options);
darkmode.showWidget();
</script>
<script>
hljs.initHighlightingOnLoad();
</script>
